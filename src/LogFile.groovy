import sun.awt.shell.ShellFolder

/**
 * Created by rbe on 29/03/2017.
 */

class LogFile {


/*var c =db.Test.aggregate(
        [
                {$group : {_id :{Client: "$clientID",Day:"$Date"}, total : { $sum : "$sc_byte" }}}

        ])
var c =db.Test.aggregate(         [                 {$group : {_id :{Client: "$clientID",Day:"$Date"}, total : { $sum : "$sc_byte" }}}          ])
while ( c.hasNext() ) printjson( c.next() )


var c =db.Test.aggregate(         {                 {$group : {_id :{Client: "$clientID",Day:"$Date"}, total : { $sum : "$sc_byte" }}} },{sort{Day:1}   })


*/


    static void main(String[] args) {
  /*      def lst = []
        def clst = []
        def Nlst  = [];
        //def result = [:];
        int i = 0, j = 0
        File file = new File("/Users/rbe/Desktop/Costs") {}
        file.eachLine { String line ->
            if (line.startsWith("2")) {
                lst = line.split()
                clst = lst[7].split("/")
                lst[0] + "----" + lst[1] + "----" + clst[3] + "----" + lst[3]
                println Nlst = [lst[0], lst[1], clst[3], lst[3]]

                    }
    */            }
                }




